# Contoh konfigurasi environment. Copia file ini menjadi `.env` dan isi nilai.
# JANGAN commit `.env` ke Git (lihat .gitignore).

SECRET_KEY=change-me-to-a-long-random-string
DATABASE_URL=sqlite:///ccs.db
FLASK_APP=run.py
FLASK_ENV=development